# Version History & Policy

This log records the formal releases, deployments, and the 3-level versioning strategy enforced across the **Airclone** project.

## 📌 Versioning Strategy (3-Level System)

All versioning follows the semantic hierarchy configured within the `/Test-and-Deploy` pipeline:

1. **Level 1 (Major)**: User-directed primary versions (e.g., `1.02.003` -> `2.00.000`). Triggered for fundamental architectural updates, paradigm shifts, or major product milestones. Resets both minor and patch levels to double/triple zero padding.
2. **Level 2 (Minor)**: New feature versions (e.g., `1.02.003` -> `1.03.003`). Adds `.01` to Level 2 versioning (allowing up to 99 level 2 versions), while preserving the patch level as-is. Prompted and confirmed when introducing discrete new features, capabilities, or major screen flows.
3. **Level 3 (Patch)**: Automated deployment versions (e.g., `1.02.003` -> `1.02.004`). Adds `.001` to Level 3 versioning (allowing up to 999 level 3 versions), while preserving the minor level as-is. Automatically bumped on every routine code deployment or patch push if no major/minor bump is specified.

---

## 📈 Release Log

| Version | Date | Level | Deployer | Key Release Highlights / Milestones |
|---|---|---|---|---|
| **v0.0.0** | 2026-06-28 | — | bootstrap | Repo initialized; documentation architecture + research scaffold established (pre-code). |
| **v0.1.0-alpha.1** | 2026-06-28 | Minor | CI (windows-latest) | First code: Flutter desktop shell — provisions/starts the rclone engine and browses remotes + local disk. Windows reference build. |
| **v0.1.0-alpha.2** | 2026-06-28 | Patch | CI (windows-latest) | Add-remote wizard (dynamic forms + OAuth state-machine), delete remote, and encrypted-config password gate. |
| **v0.1.0-alpha.3** | 2026-06-28 | Minor | CI (windows-latest) | Dual-pane + drag-drop transfers, jobs panel, file operations, bandwidth limit, settings/theme + update check. Built via a 4-agent workflow. |
| **v0.1.0-alpha.4** | 2026-06-28 | Patch | CI (windows-latest) | Fix blank-pane StackOverflow (folder-row DragTarget closure); BrowserPane regression test. |
| **v0.1.0-alpha.5** | 2026-06-28 | Minor | local + CI | File/photo previews, editable path bar, right-click menus + clipboard, back/forward history + filter + keyboard shortcuts (REM #9), deselect remote, public-link capability gating. First batch built on the local toolchain. |
| **v0.1.0-alpha.6** | 2026-06-28 | Minor | local + CI | Video/audio previews (media_kit) and sortable columns (Name/Size/Modified). |
| **v0.1.0-alpha.7** | 2026-06-28 | Minor | local + CI | Explorer redesign Phase 1: per-pane **grid view** with a type/kind icon system, on-demand **thumbnails** over rclone (off by default, per-remote toggle, disk-cached, visible-window-only), and live grid-density controls. Built via a 3-agent workflow. |
| **v0.1.0-alpha.8** | 2026-06-28 | Minor | local + CI | Explorer redesign Phase 2: tabbed **Inspector** rail (Overview/More, quick actions, Ctrl+I), date-grouped **Media gallery** view (3rd view mode), and immersive **Quick Look** (Space) with ←/→ navigation + reusable `PreviewContent`. Built via a 3-agent workflow. |
| **v0.1.0-alpha.9** | 2026-06-28 | Minor | local + CI | Explorer feel: **local-filesystem browsing** (drives + Home/Desktop/Documents/Downloads/Pictures/Videos/Music via the rclone `local` backend) surfaced in a **grouped sidebar** (Locations / Cloud), and a **single-pane explorer layout by default** with a dual-pane (commander) toggle in the top bar. Android release build set best-effort (`continue-on-error`) pending a real fix. |
| **v0.1.0-alpha.10** | 2026-06-28 | Patch | local + CI | **Android build fix (attempt 1, reverted)**: a root-Gradle `compileSdk` override errored on AGP 9. Superseded by alpha.11's dependency bump. |
| **v0.1.0-alpha.11** | 2026-06-28 | Minor | local + CI | **Collapsible sidebar** (Locations / Disks / Cloud each toggle via a chevron, persisted) and **editable Locations** (native folder picker +, drag-drop a folder to add, remove from sidebar). **Android build fixed** the right way: bump `desktop_drop` 0.6.1 → 0.7.1 (compiles against compileSdk 34+). First all-green release (windows/macOS/linux/android). |
| **v0.1.0-alpha.12** | 2026-06-28 | Minor | local + CI | **Thumbnails auto-generate** for pictures **and videos** by default. Local folders always on; cloud remotes on-by-default with a per-remote disable toggle (bandwidth). Video thumbnails capture a keyframe via libmpv (media_kit) headlessly, cached to disk, with graceful icon fallback. |
| **v0.1.0-alpha.13** | 2026-06-28 | Patch | local + CI | **Instant right-click menu** (custom popup route — no fsinfo fetch blocking, no scale-in animation; capability read from cache) and a **resizable + hideable sidebar** (drag the divider; toggle from the top bar). Kicked off the Explorer-native UX track in the backlog. **Relicensed to GNU AGPLv3** (history squashed to a single AGPLv3 commit). |
| **v0.1.0-alpha.14** | 2026-06-28 | Minor | local + CI | **Explorer-style two-row pane header**: an address row (nav + breadcrumb + filter) and a **command bar** (New · Cut/Copy/Paste · Rename · Delete · **Sort ▾** · **View ▾**). The **View menu** carries icon-size presets (Extra-large/Large/Medium/Small icons · List · Media) + the Thumbnails toggle. |
| **v0.1.0-alpha.15** | 2026-06-28 | Minor | local + CI | **Advanced transfer dialog** (Copy/Move/Sync · skip-newer/existing · compare · Include/Exclude/Filter tabs · live rclone-cmd preview · Dry-run/Run) on the command bar, and a **live statistics strip** (aggregate speed/ETA + per-file progress from `core/stats`) in the transfers dock. Authored via a 3-agent parallel workflow. (Folder-preview compositor landed, wiring next.) |
| **v0.1.0-alpha.16** | 2026-06-28 | Minor | local + CI | **Folder previews** — folders in grid view render a composite thumbnail of their first images (when thumbnails are on). Plus a safe **orphaned-engine reap**: the spawned `rclone rcd` PID is recorded and the previous one is killed on next launch (targeted, never the user's other rclone). Authored via a 2-agent workflow. |
| **v0.1.0-alpha.17** | 2026-06-28 | Minor | local + CI | **Tabs** — each pane holds multiple tabs, each an independent session (remote/path/selection/view + its own back/forward history). New-tab **+** in the address row, a tab strip when >1 tab, **Ctrl+T** new tab / **Ctrl+W** close tab. Internal session model keeps `Notifier<BrowserState>` so all call sites are untouched. |
| **v0.1.0-alpha.18** | 2026-06-28 | Patch | local + CI | **Fix video thumbnails** — the headless keyframe capture now attaches a `VideoController` and plays muted until the first frame renders before `screenshot`, so mp4/mov/etc. show real previews (libmpv needs a video output to render a frame). Images were already fine. |
| **v0.1.0-alpha.19** | 2026-06-28 | Minor | local + CI | **Encrypted preview cache + cache controls.** Thumbnails/folder previews are AES-256-GCM sealed on disk; key via PBKDF2 from the **rclone config password** (so no password ⇒ unreadable cache), else a per-**remote-name** key (obfuscation only). Settings → **Preview cache**: shows size, **Clear cache**, and **Keep cache in memory only** (no disk writes). |
| **v0.1.0-alpha.20** | 2026-06-28 | Minor | local + CI | UX wins: **Download now prompts for the folder** (and remembers it) instead of assuming Downloads — Settings → **Downloads** has a default folder + "always ask" toggle. **Type-to-navigate** (type a name to jump-select + scroll). **Richer status bar** (active-pane item count · selection + size · free/total space via `operations/about`). |
| **v0.1.0-alpha.21** | 2026-06-28 | Patch | local + CI | **Fix double context menu** — right-clicking a file no longer also opened the empty-space (New folder) menu. Switched all secondary-tap handlers from `onSecondaryTapDown` (fires for every nested handler) to `onSecondaryTapUp` (only the gesture-arena winner). |
| **v0.1.0-alpha.22** | 2026-06-28 | Minor | local + CI | **Easy/Advanced mode + Saved tasks.** Settings → **Advanced mode** reveals the advanced transfer dialog (gated off in easy mode) + a **Saved tasks** panel (top bar): create a From→To task with options, then **Run**/Delete it later. (Queue + scheduler still pending — scheduler needs background execution.) |
| **v0.1.0-alpha.23** | 2026-06-28 | Minor | local + CI | **Morphing address bar + resizable columns + engine flags.** The path bar now renders **clickable breadcrumbs** (remote chip › folder › folder, with middle-overflow into a `…` menu) and **click-to-edit** into a text field (Enter navigates, Esc reverts). **Details columns are user-resizable** — drag the divider on the Size/Modified headers (persisted), rows track the same widths. Advanced: **global engine flags** (Settings → Engine flags, e.g. `--transfers 8 --fast-list`) with **Apply & restart engine**. Authored via a 3-agent parallel workflow. |
| **v0.1.0-alpha.24** | 2026-06-28 | Minor | local + CI | **Transfer concurrency queue.** Advanced → **Concurrent transfers** caps how many run at once (default Unlimited preserves prior behavior); the rest sit **Queued** in the transfers dock and dispatch automatically as slots free. Queued jobs are cancelable; the dock header shows an `N queued` count. Covered by new unit tests (gating, completion-pump, queued-cancel). |
| **v0.1.0-alpha.25** | 2026-06-28 | Minor | local + CI | **Native window backdrop (opt-in).** Settings → **Window background**: System default (current), Solid, **Mica (Windows 11)**, or **Acrylic** — applied via `flutter_acrylic`, persisted, with the app painting transparently behind the OS material when active. Best-effort (silent no-op where unsupported); defaults to System default so nothing changes unless enabled. First cut of the native-chrome track — surface translucency tuning to follow. |
| **v0.1.0-alpha.26** | 2026-06-28 | Minor | local + CI | **Explorer keyboard shortcuts + per-location view memory.** Active pane now responds to **F2** (rename), **Delete**, **Enter** (open folder / Quick Look file), **Ctrl+A** (select all), **Esc** (clear selection). Each remote **remembers how you last viewed it** (list/grid/media · sort column + direction · grid density), restored on open and persisted. Covered by new unit tests (ViewPref round-trip + remember/no-op). |
| **v0.1.0-alpha.27** | 2026-06-28 | Patch | local + CI | **Clipboard keyboard shortcuts** — **Ctrl+C** / **Ctrl+X** / **Ctrl+V** copy/cut/paste the active pane's selection via the existing shared clipboard (move on paste for a cut), completing the Explorer key map. Focused text fields still handle these keys natively. |
| **v0.1.0-alpha.28** | 2026-06-28 | Minor | local + CI | **Drag a local file OUT to the OS** (first cut). Select a file on a local disk → the **Details** preview becomes draggable; drag it to Explorer/Finder/desktop to **copy** it there (`super_drag_and_drop`). Lives on the inspector preview (not the rows) so the in-app pane drag is untouched; cloud files aren't draggable yet (needs on-drop download). **Build note:** desktop builds now require a **Rust toolchain** (cargokit); CI installs it via `dtolnay/rust-toolchain`. |
| **v0.1.0-alpha.29** | 2026-06-28 | Patch | local + CI | **Fix drag self-reupload.** Dragging a file inside a pane and releasing it over the same folder (or dropping a remote's root items back onto that remote) no longer copies the file onto itself — same-location and folder-into-itself drops are now no-ops. (In-app drag only; OS drag-out from the file rows is still a separate, larger change — see backlog.) |
| **v0.1.0-alpha.85** | 2026-07-02 | Minor | local + CI + **released** | **Native desktop by default.** Skin auto-matches the host OS (Explorer/Finder/GNOME; Airclone opt-in); Home quick-access start page (Favorites/Recents/This computer/Cloud tiles); F5/Backspace/Ctrl+L keyboard parity; Explorer status-bar view toggles. Found+fixed a long-standing shortcut bug: plain-key/clipboard bindings stole keystrokes from text fields (empirically probed, regression-tested). |
| **v0.1.0-alpha.84** | 2026-07-02 | Minor | local + CI + **released** | **Android works: bundled rclone engine + phone-first UI.** rclone v1.74.3 cross-compiled per ABI into the APK (fake-jniLib librclone.so, exec from nativeLibraryDir) spawning the same loopback rcd as desktop; phone shell (bottom nav, locations, touch grammar: tap=open, long-press=menu); All Files Access flow; per-ABI release APKs (arm64 45 MB). 5-lens adversarial review: 15 confirmed findings fixed (allowBackup cred leak, rc-flag precedence, rcd death detection, mojibake repair, pre-Android-11 storage). Known: background transfers + stable signing deferred. |
| **v0.1.0-alpha.83** | 2026-07-01 | Minor | local + CI + **released** | **Checksums · pause queue · ETA · open serve URL · mount cache refresh.** Five gap-audit features vetted by a 14-agent adversarial review (12 findings fixed pre-commit, incl. a live-reproduced HIGH: `vfs/refresh` requires the STRING `'true'` for `recursive` — a JSON bool gets HTTP 500). Checksums via `operations/stat {showHash}` (local files limited to md5/sha1/sha256, scrollable list, friendly not-found/timeout); queue pause/resume; per-job ETA; open HTTP/WebDAV serves in the browser (loopback fallback); async mount cache refresh with inline result. +11 tests (205 total). Also: **v0.1.0-alpha.82 released publicly** with the first-ever Android APK asset (119 MB, all ABIs). |
| **v0.1.0-alpha.82** | 2026-06-30 | Minor | local + CI (branch) | **Per-file progress in a job row.** The poller already fetched `core/stats` per job group and read `transferring` only to sum sizes, then discarded it; now a running multi-file job lists up to 3 in-flight files (name + %) under its bar. `TransferItem` moved to the model layer with a shared `listFrom` parser (also used by the engine stats panel). +4 tests (196 total). Completes the 4-pick gap-audit batch (rename, test-connection, retry, per-file progress). (branch `backlog-features`.) |
| **v0.1.0-alpha.81** | 2026-06-30 | Minor | local + CI (branch) | **Retry a failed transfer.** Failed/canceled jobs get a **Retry** (↺) button. The `Job` now retains the resolved `rcMethod`/`rcParams` it dispatched (via a shared `_dispatchResolved`), and `TransferService.retry` replays that exact call as a fresh job (new id + `_group`), leaving the original untouched. Replays only already-vetted `sync/*`/`operations/*file` calls (never `core/command`). +2 tests (192 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.80** | 2026-06-30 | Minor | local + CI (branch) | **Test a remote's connection.** Right-click a remote → **Test connection** (or Ctrl+K) actively verifies reachability: `operations/about` (shows "Reachable — X free of Y") with a root `operations/list` fallback for backends without About, and on failure surfaces the **real error** instead of the silent null `remoteAboutProvider` used to swallow. Read-only. +4 tests (190 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.79** | 2026-06-30 | Minor | local + CI (branch) | **Collision-aware rename + new folder.** A 5-agent subsystem gap-audit flagged rename as the last file-mutation path that still silently overwrote (`operations/movefile` clobbers; handlers only guarded name-unchanged). The shared name dialog now takes the sibling names and blocks submit inline ("… already exists here") for both rename and new-folder; unchanged name still allowed; no overwrite possible, no RC round-trip. Every mutation path is now conflict-aware. +3 tests (186 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.78** | 2026-06-30 | Minor | local + CI (branch) | **First-run onboarding CTA.** An empty pane used to say "Pick a remote on the left" even with zero remotes configured. Now, once the remotes list has loaded empty (no flash during load), it shows a welcome — "Connect your first remote", a one-liner (Drive/S3/Dropbox/OneDrive + "local drives are already in the sidebar"), and an **Add a remote** button. Returning users with remotes keep the neutral hint. +2 tests (183 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.77** | 2026-06-30 | Minor | local + CI (branch) | **Drag-and-drop is conflict-aware.** The last gap: in-app drag still overwrote silently. Paste + every drop target (pane, folder row, subfolder, sidebar remote root) now share one core (`transferNamesIntoFolder`) that prompts **Skip / Replace / Keep both**. Vetted by an 8-agent adversarial review (no data-loss; drop no-op guards preserved verbatim); folded in its robustness fixes — an in-flight latch against double-paste/drop, and returning `plan.isNotEmpty` so a skip-everything CUT keeps the clipboard. +2 tests (181 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.76** | 2026-06-30 | Patch | local + CI (branch) | **Subfolder paste is conflict-aware too.** Completes the paste story: a72/a73 covered pasting into the current folder; right-click **Paste onto a subfolder** now also prompts Skip/Replace/Keep-both instead of silently overwriting. The shared helper gained `pasteClipboardIntoFolder`, which lists an arbitrary target (read-only `operations/list`) to detect collisions when the listing isn't already loaded, and reuses the pane's listing when it is. +1 test (179 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.75** | 2026-06-30 | Minor | local + CI (branch) | **Recent locations in the command palette.** **Ctrl+K** now lists up to 8 recently-visited folders (history icon, "Recent" tag) after favorites — excluding the current folder and anything already pinned. A session-only, capped-at-12, newest-first list (`state/recent_locations.dart`) fed by a per-pane `ref.listen` that records each folder change. Rounds out the palette + favorites + search family. +3 tests (178 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.74** | 2026-06-30 | Patch | local + CI (branch) | **"Copy command" on the transfer preview.** The advanced transfer dialog's "rclone cmd" tab already showed the equivalent CLI; it now has a **Copy** button (flips to "Copied", resets when the command changes) + a "run this from a terminal or cron job" caption — so you can lift the exact `rclone …` invocation for scripting. +1 test (175 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.73** | 2026-06-30 | Patch | local + CI (branch) | **Unify paste (context-menu paste now conflict-aware too).** a72 made **Ctrl+V** prompt on name collisions, but the per-pane right-click **Paste** had a separate older implementation that still silently overwrote. Both now call one shared `pasteClipboardInto` helper (`ui/paste_action.dart`), so paste behaves identically — and conflict-aware — from either entry point. One implementation instead of two. analyze (0) / test (174) green. (branch `backlog-features`.) |
| **v0.1.0-alpha.72** | 2026-06-30 | Minor | local + CI (branch) | **Conflict-aware paste.** Ctrl+V'ing files whose names already exist in the destination now prompts **Skip / Replace / Keep both** instead of silently overwriting. Collisions are detected for free against the pane's loaded listing; `planPaste` resolves the choice (keep-both routes each name through a desktop-style `report (2).pdf` rename so nothing is clobbered — the gap rclone can't fill itself). Pure helpers fully unit-tested. (Storage-tiers backlog item vetted + skipped: S3 has no `settier` backend command, so it would need `core/command` — avoided.) +12 tests (174 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.71** | 2026-06-30 | Minor | local + CI (branch) | **Find duplicate files (safe client-side dedupe).** **Ctrl+K → "Find duplicate files…"** scans a folder (`operations/list {recurse, showHash}`) for content-identical copies (same size + hash), groups those at **distinct** paths, and lets you keep one per group (or skip) and delete the rest via `operations/deletefile` (unique path each → unambiguous). Native rclone dedupe needs `core/command` (arbitrary CLI) — deliberately avoided; this uses only safe primitives. Vetted by a 12-agent adversarial **safety** review: 4 findings, **zero data-loss** (keep-one/only-non-kept/unique-path invariants held); fixes folded in (non-dismissible while busy, error surfaced, guarded refresh). +12 tests (162 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.70** | 2026-06-30 | Patch | local + CI (branch) | **Review fixes for a65–a69.** Acted on a 36-agent adversarial review (4 dimensions × verify): 14 findings confirmed, security dimension clean. **HIGH:** mounted-guards on the public-link dialog's async paths (no more setState-after-dispose if closed mid-RPC); recursive search's 500 cap is now a real memory bound (one-pass filter, not build-then-trim). **LOW:** palette no longer mutates state in build + scrolls only when off-screen; public-link Copy/revoke feedback + expiry caveat; search keeps MimeType for icons + disables Search on empty query; search reveal scrolls into view + skips redundant reload; favorites drop the redundant ★ prefix and no root-pin dup; bookmark identity via `key`. +5 tests (150 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.69** | 2026-06-30 | Minor | local + CI (branch) | **Favorites (pin folders).** Pin any folder and jump straight back to it — surfaced through the **Ctrl+K** command palette ("Pin this folder to Favorites", then a **★ remote/path** entry per pin that opens + navigates there). Persisted in SharedPreferences (newest-first, dedup by fs+path). Lives in the palette rather than the OS-skinned sidebar so the native chrome is untouched. Local-only, no RC. 5 new tests (145 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.68** | 2026-06-30 | Minor | local + CI (branch) | **Recursive search (Ctrl+Shift+F).** The pane filter only matched the current folder; this finds files *anywhere beneath* it. One `operations/list {recurse:true}` over the chosen folder, then token filtering on name+path; folders-first results show name · parent · size, with a generation-guard against stale scans and a 500-row display cap. Selecting a result reveals it in the active pane (`navigateTo` + `selectOnly`). Also in the command palette ("Search this folder…"). Read-only; reuses existing RC. 3 new tests (140 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.67** | 2026-06-30 | Minor | local + CI (branch) | **Command palette (Ctrl+K).** A Spotlight-style launcher: press **Ctrl+K**, type to fuzzy-filter, ↑/↓ + Enter to run. Catalogues every action — Add/encrypt remote, Settings, Keyboard shortcuts, New tab, toggle details/sidebar/dual-pane, and (advanced + policy-gated) Saved tasks / Serve / Mount — plus a **"Go to <remote>"** entry per configured remote. Reuses the existing handlers + `remotesProvider`; no new RC surface. 6 new tests (137 total). (branch `backlog-features`.) |
| **v0.1.0-alpha.66** | 2026-06-30 | Minor | local + CI (branch) | **Keyboard shortcut cheat-sheet (F1).** Press **F1** — or the new keyboard-glyph button beside the Settings cog — for a read-only reference card of every Explorer shortcut (Navigate / Tabs & panes / Select / Edit). Surfaces the full key map shipped in a26 + a27, which until now had no in-app discovery. Pure UI, token-styled (tracks the active skin), no RC calls. (branch `backlog-features`.) |
| **v0.1.0-alpha.65** | 2026-06-30 | Minor | local + CI (branch) | **Public links — expiry + revoke + copy.** The share-link dialog (right-click "Get public link" / inspector Copy-link) now lets you pick an **expiry** (none/1h/1d/1w/1mo → `operations/publiclink {expire}`), **copy** it, and **revoke** it (`{unlink:true}`). Consolidated the two duplicate implementations into one shared dialog; backend support varies → engine error surfaced. (Chrome rework + a50–a64 now **merged to `main`**; this is on `backlog-features`.) |
| **v0.1.0-alpha.64** | 2026-06-30 | Minor | local + CI (branch) | **Bandwidth schedule (daily timetable).** The top-bar bandwidth control gains a **Schedule…** editor: define daily windows (e.g. 08:00 → 512k, 18:00 → off) and Airclone applies the active window's rate live to `core/bwlimit` on a 60s clock (rclone's live limit is a single rate, so the app holds the timetable). Persisted; honest "applies only while open". Pure `activeRate` (daily wrap) unit-tested. 5 new tests (131 total). (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.63** | 2026-06-30 | Patch | local + CI (branch) | **Storage breakdown ("what's using my space").** Tools menu → **Storage breakdown…** sizes each subfolder of the current folder (`operations/size`, fetched in parallel), sorts largest-first, and shows a relative bar + size per folder with a total — a quick "where's my space going" view. Reuses the a53 size op; no new RC surface. (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.62** | 2026-06-30 | Minor | local + CI (branch) | **Mount manager — mount remotes as drives.** An advanced-gated **"Mount as a drive"** toolbar button: pick a remote · subfolder · drive letter (or Auto) · VFS cache mode, and it mounts via `mount/mount` so the remote shows up in Explorer; a polled **mounted-drives list** (`mount/listmounts`) with unmount / unmount-all. Needs **WinFsp** (a clear banner guides install when `mount/types` is empty). Mirrors the verified Serve feature: a `mountEnabledProvider` **policy kill-switch** (enforced in `mount()`, not just UI), cache mode defaults to **writes**, never sets a shared VFS cache dir (no corruption), nothing persisted (no auto-resurrect). Built by cloning Serve after the design workflow hit a transient rate-limit. 8 new tests (126 total). (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.61** | 2026-06-30 | Patch | local + CI (branch) | **Make Sync discoverable (folder-level advanced transfer).** Fix for "where's Sync after enabling advanced?" — the Copy/Move/Sync dialog was only reachable via a selection-gated "Transfer with options…". Now the **Tools** menu (and the ⋯ overflow) has **"Copy / Move / Sync this folder…"** available without a selection, and `_advancedTransfer` is folder-aware: no selection ⇒ whole current folder → destination (the natural target for Sync / Two-way); with a selection ⇒ the selected entries. Labels show "(whole folder)" vs "(N selected)". (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.60** | 2026-06-30 | Minor | local + CI (branch) | **Edit + duplicate a remote.** A cloud remote's ⋮ menu now offers **Edit remote…** (prefills the form from `config/get`, leaves passwords blank → blank keeps the current one, saves via `config/update` MERGE) and **Duplicate remote…** (copies the config under a new name via `config/create`). Obscure-safe end-to-end: edits send only typed (plaintext) passwords with `obscure:true` (existing ones omitted → never double-obscured), duplicates pass already-obscured values through with `noObscure:true`; `answer()` routes to config/update during interactive edits. Designed by an agent that confirmed `config/get`/`config/update` MERGE + obscure rules vs rclone source. 5 new tests (119 total). (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.59** | 2026-06-30 | Minor | local + CI (branch) | **Advanced performance & safety controls.** A **Performance** group in the advanced transfer dialog: parallel **transfers**/**checkers** (`--transfers`/`--checkers`, blank = rclone default), a **sort order** dropdown (`--order-by`, e.g. size/newest-first), **Track renames** (server-side rename detection, no re-upload), and **Immutable** (abort rather than modify existing files). All map to per-call `_config` keys (ConfigInfo names confirmed vs rclone docs), one-way only, with guarded-omit JSON so simple tasks are unchanged. 3 new tests (114 total). (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.58** | 2026-06-30 | Minor | local + CI (branch) | **Transfer history — "Recent activity" tab.** The bottom transfers dock now has two tabs: **Transfers** (live stats + jobs, default) and **Recent activity** — a read-only list of recently completed transfers (rclone's rolling ~100, via `core/transferred`) with a per-file success/error indicator + the error text + size, plus refresh. New `TransferredItem` model parses the exact rclone snapshot shape (verified vs master source — the public docs page is stale; Go zero-time → null). 7 new tests (111 total). (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.57** | 2026-06-30 | Minor | local + CI (branch) | **Serve / share a remote on the LAN.** Turn any remote into a local **HTTP/WebDAV/FTP/SFTP/DLNA** server (cast to a TV, mount on a phone…) from an advanced-gated toolbar button: pick remote · protocol (from serve/types) · this-device-only-vs-LAN · port · auth, with a pre-start network warning, and a polled **running-servers manager** (reachable URL + this-device/LAN labels · copy · Stop · Stop all). **Security enforced in `start()` itself**, not just the UI: loopback by default, **mandatory user+pass off-loopback**, DLNA-no-password acknowledgement, a `serveEnabledProvider` **policy kill-switch** (read at call-time), whitelisted params (no rc creds/config password), never auto-started/persisted. Designed via a design→correctness+**security** verify workflow (RC confirmed vs rclone source). 9 new tests (104 total). **Completes all four picked feature tracks.** (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.56** | 2026-06-30 | Minor | local + CI (branch) | **Encrypt-a-remote (crypt) wizard.** Wrap any cloud remote with **client-side encryption** from the CLOUD "+" menu (Add a remote… / Encrypt a remote…): pick the base remote, set filename/dir-name encryption + a password (+ optional salt), and it creates a `crypt` remote (`config/create opt:{obscure}` — rclone obscures server-side) + best-effort `cryptcheck`. **Security-first:** the password is never persisted or logged by Airclone (held only in transient controllers/args, never in provider state), with an unconditional nudge to enable rclone config encryption. Designed via a design→correctness+**security** verify workflow (RC params confirmed vs rclone source). 5 new tests (95 total). (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.55** | 2026-06-30 | Minor | local + CI (branch) | **Two-way sync (bisync) — now usable.** The guarded UI on top of a54's engine: an **advanced-gated "Two-way sync" mode** (the transfer dialog isn't otherwise gated) with a conflict-resolution control (default keep-both), first-run baseline-winner, a **max-delete % safety slider**, and check-access/create-empty-dirs. Saved as a task, its first run opens a **baseline confirm** that names Path1(From)/Path2(other) + which side wins and offers **Dry-run-first**; a job-success hook then records the baseline so later runs are normal two-way syncs (never flipped on a dry-run). Rows show a **"Needs first run"** badge; the scheduler never auto-runs an unattended destructive resync. Safe-by-default even if the baseline flip is missed (manual runs re-confirm; scheduler skips). 1 new test (90 total). (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.54** | 2026-06-29 | Patch | local + CI (branch) | **Two-way sync (bisync) — engine layer.** The deterministic, fully-tested plumbing for bidirectional sync: `TransferMode.bisync`, bisync settings on `TransferOptions` (resync mode, conflict resolve/loser/suffix, max-delete %, check-access, baseline-established) with back-compatible JSON, an early-returning `buildRcCall` branch emitting `sync/bisync` top-level params (no `_config` leak; resync only on first baseline), a CLI preview, and a scheduler guard that never auto-runs a destructive first `--resync` unattended. **Inert until the UI lands (a55)** — no destructive exposure yet. Designed via a design→correctness+safety verify workflow that confirmed every RC param against rclone source. 11 new tests. (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.53** | 2026-06-29 | Minor | local + CI (branch) | **Folder Tools — Compare · Upload-from-URL · Folder size · Empty trash.** A new **Tools** menu in the command bar (and the ⋯ overflow) adds four RC-powered actions: **Compare with other pane** (verify two locations by hash/size → identical/different/missing buckets, with a download fallback for hashless backends, via `operations/check`); **Upload from URL** (stream a link straight into a remote, `operations/copyurl`); **Folder size** (`operations/size`); **Empty trash** (reclaim space, `operations/cleanup`). First batch of the quick-wins bundle from the mined build queue. RC contracts verified against rclone source. 3 new tests. (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.52** | 2026-06-29 | Minor | local + CI (branch) | **Recoverable transfers + RC-grounded backlog.** A **"Keep replaced files"** toggle in the transfer-options dialog: overwritten/deleted destination files are renamed with a `.replaced` suffix (`--suffix --suffix-keep-extension`) instead of being lost, making **Move/Sync recoverable**. First build from the deep-dive **feature-mining** research workflow, which also produced a durable **RC-grounded build queue** in the backlog (16 GUI-able rclone capabilities — bisync, compare/verify, crypt wizard, cleanup/empty-trash, serve, mounts, storage analysis… — each with exact RC mechanisms + security invariants, prioritized by value÷effort). 5 new tests. (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.51** | 2026-06-29 | Patch | local + CI (branch) | **Easier advanced mode + grouped settings.** The **Advanced mode** toggle is now a prominent card **pinned at the top** of Settings (accent-tinted when on; copy mentions saved + scheduled tasks), so the gate to power-user features is easy to discover and flip. Settings sections are grouped under lightweight headers (Appearance · Transfers · Engine · Storage & updates). Every control preserved, advanced gate intact; the heavier category-rail refactor was deliberately skipped (verifier flagged it cosmetic/regression-risky). (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.50** | 2026-06-29 | Minor | local + CI (branch) | **Scheduled tasks.** Saved tasks can now repeat on a **schedule** — Interval (every N min/hours), Daily at a time, or Weekly on chosen days — driven by an in-app scheduler (one app-lifetime 30s timer, engine-ready gated, stamps last-run before kickoff so it never double-fires). The Saved-tasks dialog gains a per-task alarm button + schedule editor (presets, time picker, weekday chips) and a status line ("Every 6 hours · next today 18:00" / "due now"). **Honest:** it only runs while Airclone is open; a missed slot catches up once on next launch (no OS background service). `TransferTask` extended back-compatibly (nullable schedule/lastRun, copyWith); pure `isDue`/`nextRun` unit-tested (17 new tests). Designed via a 6-agent audit→verify workflow. (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.49** | 2026-06-29 | Patch | local + CI (branch) | **Bugfixes — readable popups + responsive toolbar.** (1) Settings dropdowns (and the toolbar menus) rendered **see-through** over a Mica/Acrylic backdrop because the translucent path drops `canvasColor`; now all popup surfaces are forced **opaque** (`menuTheme`/`popupMenuTheme`/`dropdownMenuTheme` + `dropdownColor` on the legacy `DropdownButton`s). (2) When the window got **small**, toolbar verbs became unreachable; the command bar now **collapses into a `⋯` overflow** below 600 px (with Sort reachable inside it) and the OS-skin search **shrinks responsively** (120–220 px) instead of crowding out the breadcrumb. Airclone's fixed look preserved. (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.48** | 2026-06-29 | Minor | local + CI (branch) | **Per-skin chrome — Finder unified toolbar + OS de-brand.** Three new `SkinChrome` flags. **Finder** (macOS skin) collapses its hoisted toolbar into a **single Finder-style row** — Back/Forward · breadcrumb title · a **segmented view switcher** (List·Icons·Gallery) · size/Sort menus · a **⋯ overflow** holding the file verbs · search. All **OS skins de-brand the top bar** (drop the "Airclone" wordmark + version, flatten the surface) so the window reads like a native file manager, and gain the **segmented view switcher** in their command bar. Designed via a 6-agent workflow (audit→synthesize→2 adversarial verifiers) that caught a dual-pane regression — the unified row is gated on `unifiedToolbar && hoisted`, so dual-pane/Airclone are untouched. (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.47** | 2026-06-29 | Minor | local + CI (branch) | **Per-skin chrome — toolbar above the sidebar.** Explorer/Finder put their toolbar in a **full-width band across the top**, with the sidebar starting *below* it — Airclone's sidebar instead ran the whole left edge. Now the OS skins (single-pane) **hoist the active pane's toolbar to a top band**, so the sidebar no longer reaches the top. New `SkinChrome.toolbarAboveSidebar`; `BrowserPane.showToolbar`; public `PaneToolbar`; `_ExplorerArea` does the hoisting (engine-ready gated). Airclone + dual-pane unchanged. (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.46** | 2026-06-29 | Minor | local + CI (branch) | **Per-skin chrome — Explorer toolbar.** Extended `SkinChrome` to the pane toolbar: the **Windows Explorer** skin now drops the active-pane dot, shows a **labelled "New"** button, a **wide always-on Search** field, and a far-right **Details** pane toggle — closer to Explorer's command bar. Finder/GNOME also drop the dot; Finder widens search. Airclone unchanged. (branch `explorer-finder-chrome`.) |
| **v0.1.0-alpha.45** | 2026-06-29 | Minor | local + CI (branch) | **Per-skin chrome — sidebar makeover.** New `SkinChrome` delegate (folded into the theme) drives sidebar presentation per skin: **Finder** gets the signature **solid accent-fill selection pill with white text**; **Explorer/GNOME** get an inset rounded pill; **Airclone** keeps its left accent bar. OS skins use **Title-Case** section headers (e.g. "Locations") instead of ALL-CAPS, and **single-line** tiles (drop the rclone-type subtitle). First increment of the Explorer/Finder layout-matching effort (on branch `explorer-finder-chrome`; `main` snapshotted at `snapshot-pre-chrome-rework`). |
| **v0.1.0-alpha.44** | 2026-06-29 | Minor | local + CI | **macOS Finder + Linux GNOME skins.** Both optional skins now have their own **palettes** (Finder: soft grays + macOS blue `#0A84FF`/`#007AFF`; GNOME/Adwaita: Adwaita grays + blue `#3584E4`/`#78AEED`) via `forSkin`, **dividerless rounded rows** (Finder 24px, GNOME 38px) like the modern file managers, and **rounded sidebar selection**. Finder also gets the **colored folder icons** (GNOME stays monochrome/symbolic). Picks up SF Pro / Adwaita Sans fonts. Airclone default untouched. |
| **v0.1.0-alpha.43** | 2026-06-29 | Minor | local + CI | **Windows Explorer skin — sidebar.** With the Explorer skin on, the sidebar now reads like Quick Access: **coloured known-folder icons** (blue Desktop/Documents, green Downloads, teal Pictures, pink Music, blue Videos) and a **rounded selection fill** (no left accent bar). Other skins keep the monochrome + left-bar look. (Chrome work continues — command bar, Mica translucency next.) |
| **v0.1.0-alpha.42** | 2026-06-29 | Minor | local + CI | **Windows Explorer skin — colors.** The Explorer skin now has its **own Win11 palette** (neutral grays + the Windows blue accent: `#202020` surfaces / `#4CC2FF` dark accent, `#F3F3F3` / `#005FB8` light) via `AircloneColors.forSkin(skin, brightness)`, so it actually *reads* as Explorer instead of reusing the Airclone blue. Other skins still fall back to the Airclone palette. (Also fixes the stale `alpha.40` version label — that build was made one step before the version bump.) |
| **v0.1.0-alpha.41** | 2026-06-29 | Minor | local + CI | **Windows Explorer skin — row styling.** With the Windows Explorer skin selected, the Details list now drops row dividers and uses a **rounded selection + hover highlight** (new `rowDividers` token), on top of the tighter rows (28px) + Segoe UI Variable already in place — a recognizably Win11 file-list feel. Airclone/other skins keep their dividers. File rows are now hover-aware (`_FileRow` → stateful). |
| **v0.1.0-alpha.40** | 2026-06-28 | Minor | local + CI | **Skin selector — pick a look (first visible cut).** Settings → **Skin** now switches between **Airclone** (default) and optional **Windows Explorer / macOS Finder / Linux (GNOME)** skins. Each skin drives the UI **font, density, and list-row height** through `SkinTokens`, so the explorer visibly gets denser (Explorer/Finder) or roomier (GNOME). Deeper per-skin styling (toolbar layout, sidebar, selection shape, window chrome) comes next, guided by feedback. |
| **v0.1.0-alpha.39** | 2026-06-28 | Patch | local + CI | **Skin foundation (no visual change).** Added the architecture for optional per-OS skins: a `Skin { airclone (default), windows, macos, gnome }` enum, a persisted `skinProvider` (defaults to Airclone), and a `SkinTokens` bundle (font, density, row-height, selection radius) folded into `AircloneTheme`. `AppTheme.build(skin, brightness)` now drives the theme; the no-op `AircloneTheme.lerp` is fixed to interpolate. The **default Airclone skin reproduces today's exact look** — this ships zero visible change. Unit-tested. (Next: route widgets through the tokens + add the Settings selector, then the actual Explorer/Finder/GNOME skins.) |
| **v0.1.0-alpha.38** | 2026-06-28 | Patch | local + CI | **Engine-flag preset chips (discoverability 2/3).** Settings → Engine flags now has one-tap **toggle chips** for the common flags (`--fast-list`, `--transfers 8`, `--checkers 16`, `--no-traverse`) that compose into the free-text field (still the source of truth) — no need to know the syntax. Pure `toggleEngineFlag`/`hasEngineFlag` helpers, unit-tested. |
| **v0.1.0-alpha.37** | 2026-06-28 | Minor | local + CI | **Discoverable advanced transfer options (1/3).** The advanced Copy/Move/Sync dialog is no longer locked behind Advanced Mode — a **"Transfer with options…"** button sits in the command bar whenever files are selected, and it works in single-pane mode too (picks a destination if there's no second pane). The dialog now **teaches the rclone flag** on each control: compare shows `--size-only`/`--checksum`, and the Include/Exclude/Filter fields are labelled with `--include`/`--exclude`/`--filter` + tooltips. Covered by widget tests. (Settings reorg + engine-flag preset chips next.) |
| **v0.1.0-alpha.36** | 2026-06-28 | Patch | local + CI | **Auto-refresh after a transfer.** A dropped/uploaded/pasted file no longer needs a manual refresh to appear — when a transfer **job finishes** (success), both panes re-list automatically. (Transfers are async rclone jobs that complete a moment after the drop, so the listing is refreshed on completion via a `jobsControllerProvider` listener.) |
| **v0.1.0-alpha.35** | 2026-06-28 | Patch | local + CI | **Restore OS→app upload + add drag auto-scroll.** After the alpha.34 migration, `super_native_extensions` owns native drops, so the old `desktop_drop` upload stopped firing — OS-file drops are now handled inside the native `DropRegion` (read each dropped `fileUri` → upload), and `desktop_drop` is removed entirely. Also: dragging an in-app file near the **top/bottom edge now auto-scrolls** the list so you can reach an off-screen folder to drop into (Explorer-like). Sidebar "drag a folder here to add it" also moved to the native region. |
| **v0.1.0-alpha.34** | 2026-06-28 | Minor | local + CI | **Whole-file drag — unified on the native engine.** Replaced the app's in-app Flutter `Draggable`/`DragTarget` with `super_drag_and_drop` across list/grid/media rows + folder/pane/sidebar drop targets. Now you grab the **entire file** and drop it **anywhere**: onto a folder/pane/sidebar (in-app copy) **or** onto Explorer/Finder/desktop (local files copy out) — one gesture. Also **fixes the "view jumps to top on drag"** (that was Flutter's `Draggable`; the native drag doesn't do it). OS-file drops from outside still upload via `desktop_drop`. New `PaneDragData` JSON round-trip carries the in-app payload as native `localData`; covered by unit tests. |
| **v0.1.0-alpha.33** | 2026-06-28 | Patch | local + CI | **Remove the drag-start diagnostic toast** (it confirmed the gesture fires — its job is done). Also isolates whether that toast's mid-drag Scaffold layout change was behind the "view jumps to top on drag" report, ahead of the full whole-row drag migration. |
| **v0.1.0-alpha.32** | 2026-06-28 | Minor | local + CI | **Drag a local file OUT to the OS — from a row grip.** Each local file row now has a small **drag handle at its left edge**; drag it to Explorer/Finder/desktop to copy the file out (`super_drag_and_drop`). The handle is overlaid OUTSIDE the in-app `Draggable`, so pane-to-pane drag is untouched. When the drag gesture initiates it shows a brief **"Dragging … out"** toast — a deliberate diagnostic (an OS drag can't be auto-tested) so we can tell a gesture-not-firing problem from an OS-drop problem. Re-adds the **Rust** build requirement (CI installs it). _Drag is the one thing the agent can't verify on screen — needs the user to confirm._ |
| **v0.1.0-alpha.31** | 2026-06-28 | Patch | local + CI | **Fix "Show in File Explorer" opening the Desktop.** rclone's local fs hands out forward-slash paths (`C:/…`), but `explorer.exe /select,` only parses **backslashes** — given forward slashes it silently ignored `/select` and opened the default location. The Windows reveal command now converts `/`→`\`. Locked with a regression unit test. |
| **v0.1.0-alpha.30** | 2026-06-28 | Minor | local + CI | **Official OS interop replaces native drag-out.** Removed `super_drag_and_drop` (community + Rust/cargokit build burden + a drag gesture no automated test can verify) — **desktop builds no longer need a Rust toolchain**. In its place, **verifiable, first-party** actions for **local** files on the right-click menu + Details panel: **Open with default app** (`url_launcher`), **Show in File Explorer/Finder** (documented OS commands via a pure, unit-tested per-OS argv builder), and **Copy path** (`Clipboard`). Download stays for cloud. Backed by **16 new unit tests** (argv per OS, explorer-exit-code policy, Linux dbus→xdg-open fallback, URI build, clipboard). Grounded in a verified research pass: **Flutter has no official drag-out** (its own docs redirect to the community package). |
